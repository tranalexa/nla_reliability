.venv/
__pycache__/
*.pyc
.DS_Store

# Derived data — regenerate via Modal + pull_from_modal.py
data/runs/**/*.parquet
data/runs/**/*.csv
data/runs/**/*.npy

# Derived reports — regenerate via build_synthesis_tables.py
reports/*.csv
reports/*.tex

# Derived figures — regenerate via generate_figure_bundle.py + the notebook
figures/
figures_bundle/*
!figures_bundle/.gitkeep
figures_bundle */

# Notebook side effects (executed outputs land here)
notebooks/.ipynb_checkpoints/
notebooks/*.pdf

# Bundles / archives
*.zip

# Legacy stray pulls at repo root (covered by data/ rules but listed explicitly)
/descriptions.parquet
/selfdescribe_400.csv
/activations_layer32*.parquet
/activations_layer32*.npy
